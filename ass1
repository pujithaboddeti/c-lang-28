#include<stdio.h>
main()
{
    int s,e,n,i,count,count2=0;
    scanf("%d %d",&s,&e);
    for(n=s;n<=e;n++)
    {
        count=0;
        for(i=1;i<=n;i++)
        {
            if(n%i==0)
            {
                count++;
            }
        }
        if(count==2)
            count2+=1;
    }
    printf("count of primes:%d",count2);
}
